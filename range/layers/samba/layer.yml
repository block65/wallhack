packages:
  - samba
  - samba-common-tools
configs:
  - etc/samba/smb.conf
start: |
  mkdir -p /shared
  adduser -D admin 2>/dev/null || true
  printf "admin123\nadmin123\n" | smbpasswd -a -s admin 2>/dev/null || true
  smbd -F --no-process-group &
